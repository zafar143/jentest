node{
	stage('Build') {
		echo "Running job in build stage"
	}
	stage('Test') {
		echo "Running job in test stage"
	}
	stage('Deploy') {
		echo "Running job in deploy stage"
	}
	stage('Archive') {
		echo "Running job in build stage"
	}
}
